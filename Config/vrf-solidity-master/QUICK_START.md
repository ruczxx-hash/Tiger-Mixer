# VRF 测试快速开始 🚀

## 最简单的 3 步开始

### 1️⃣ 初始化项目（首次运行）

```bash
cd /home/zxx/Config/vrf-solidity-master
./run_tests.sh setup
```

### 2️⃣ 运行测试

```bash
# 方式 A: 使用便捷脚本（推荐）
./run_tests.sh simple

# 方式 B: 使用 Truffle 命令
truffle test test/test_vrf_simple.js
```

### 3️⃣ 查看结果

测试通过后会显示：
```
✅ 测试 1 完成：验证成功！
✅ 测试 2 完成：3/3 个证明验证成功！
...
6 passing (12s)
```

---

## 📋 常用命令速查

### 使用便捷脚本

```bash
./run_tests.sh simple    # 简单测试（最快，推荐）
./run_tests.sh c-gen     # C 生成器集成测试
./run_tests.sh all       # 所有测试
./run_tests.sh gas       # Gas 消耗分析
./run_tests.sh help      # 显示帮助
```

### 使用 Truffle 命令

```bash
truffle test                              # 运行所有测试
truffle test test/test_vrf_simple.js      # 运行简单测试
truffle test test/test_vrf_with_c_generator.js  # 运行 C 集成测试
npm run gas-analysis                      # Gas 分析
```

---

## 🔍 测试文件说明

| 文件 | 说明 | 推荐度 |
|------|------|--------|
| `test_vrf_simple.js` | 基础功能测试，使用预定义测试向量 | ⭐⭐⭐⭐⭐ |
| `test_vrf_with_c_generator.js` | C 程序集成测试，跨平台验证 | ⭐⭐⭐⭐ |
| `vrf.js` | 原有完整测试套件 | ⭐⭐⭐ |

---

## ❓ 遇到问题？

### 问题：依赖未安装

```bash
./run_tests.sh setup
```

### 问题：合约未编译

```bash
./run_tests.sh compile
```

### 问题：环境混乱

```bash
./run_tests.sh clean   # 清理并重装
```

### 问题：C 程序不可用

不用担心！简单测试不需要 C 程序：
```bash
./run_tests.sh simple
```

---

## 📚 详细文档

查看完整指南：[TEST_GUIDE.md](TEST_GUIDE.md)

---

## 💡 提示

- ✅ 首次运行使用 `./run_tests.sh setup`
- ✅ 日常测试使用 `./run_tests.sh simple`（最快）
- ✅ 完整验证使用 `./run_tests.sh all`
- ✅ 性能分析使用 `./run_tests.sh gas`

---

**祝测试顺利！** 🎉

















